function divisivelPorTres(numero) {
  return numero % 3 === 0;
}

// Exemplo de uso:
let numero = 9; // Defina o número que deseja verificar
console.log(divisivelPorTres(numero));  // true (se for divisível por 3) ou false (se não for)
